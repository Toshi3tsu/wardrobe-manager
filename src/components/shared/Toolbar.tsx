import React from "react";

const Toolbar: React.FC = () => {
  return (
    <div className="toolbar">
      <button>保存</button>
      <button>リセット</button>
    </div>
  );
};

export default Toolbar;
