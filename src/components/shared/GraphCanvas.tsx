import React from "react";

const GraphCanvas: React.FC = () => {
  return (
    <div className="graph-canvas">
      <h2>Graph UI - 服の関係性</h2>
      {/* グラフUIライブラリ（例: react-d3-graph）をここに実装 */}
    </div>
  );
};

export default GraphCanvas;
